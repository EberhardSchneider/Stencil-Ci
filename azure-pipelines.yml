# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

pool:
  vmImage: 'Ubuntu 16.04'

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '2.x'
  displayName: 'Install Python'

- script: |
    PATH="$PATH:$HOME/vsts/.local/bin"
    pip2 install awscli --upgrade
    export AWS__ACCESS_KEY=AKIAJ737YO44ANNVS7JA
    export AWS_SCRET_ACCESS_KEY=IdKWto9p6i9ZsLJGTJ8cGdqNqE/TGjAwtFs6rVgN
    aws s3 ls
  displayName: 'aws install and check'
